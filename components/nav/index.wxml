<view class="nav-container">
    <image class="icon" bind:tap="onLeft" src="{{latest ? disLeft : left}}" />
    <text class="nav-title">{{title}}</text>
    <image class="icon" bind:tap="onRight" src="{{first ? disRight : right}}" />
</view>
